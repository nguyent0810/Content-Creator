# Core Knowledge Model

Knowledge is domain-owned. Core stores relationships, IDs, lineage, confidence, source attribution, and compatibility aliases.

A concept with the same label across domains is not automatically identical. Cross-domain equivalence requires policy approval and relationship registry entry.
